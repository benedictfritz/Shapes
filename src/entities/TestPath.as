package entities
{
    import net.flashpunk.FP;
    import net.flashpunk.Entity;

    public class TestPath extends Entity {

        public function TestPath(x:Number, y:Number):void {
            super(x, y);
        }

        override public function update():void {
            super.update();
        }
    }
}